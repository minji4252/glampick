// // 로그인 상태에서만 접근 가능한 페이지들
// import { Navigate } from "react-router-dom";

// function PrivateRoute({ isLogin, children }) {
//   return isLogin ? children : <Navigate to="/login" />;
// }

// export default PrivateRoute;
